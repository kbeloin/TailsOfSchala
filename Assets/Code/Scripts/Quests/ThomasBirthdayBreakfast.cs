using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ThomasBirthdayBreakfast : Quest
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(this.isComplete);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
